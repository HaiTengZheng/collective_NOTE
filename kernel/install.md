# Ubuntu
```bash
# intall the required packages for building the Linux kernel
sudo apt install -y \
    bison build-essential flex libncurses5-dev ncurses-dev \
    libelf-dev libssl-dev tar util-linux xz-tuils
```
